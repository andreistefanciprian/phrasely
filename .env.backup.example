# Trusted Bash configuration: quote values, especially URLs containing & or $.
DATABASE_URL='postgresql://phrasely:phrasely@localhost:5432/phrasely?sslmode=disable'
BACKUP_DIR='./backups'
PGCONNECT_TIMEOUT=10
